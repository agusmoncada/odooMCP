# ✅ ZERO DEPENDENCIES NEEDED!
# This addon uses lightweight alternatives compatible with Odoo v16

# No pip install required! The addon now uses:
# - PyPDF2 (already in Odoo v16) instead of pdfplumber
# - Pure Python stdlib instead of pandas/numpy
# - Odoo's built-in tools instead of Pillow

# Optional: Only if PyPDF2 is somehow missing from your Odoo
# PyPDF2>=1.26.0,<4.0.0

# See /requirements.txt in repo root for full documentation
